extends Control

#TODO add loading these dialog graphs
#requires converting a dialog grpah to a series of nodes and connections
#and also a variable editor interface
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
